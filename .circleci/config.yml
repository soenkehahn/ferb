version: 2
jobs:
  build:
    docker:
      - image: node:8.2.1
    steps:
      - checkout
      - run: apt update && apt install --yes golang-go
      - run: yarn install
      - run: yarn flow
      - run: ./slow-tests.sh
